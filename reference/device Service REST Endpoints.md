---
tags:
---
*   `GET /drive-u/videopipeline/{vin}`: Fetches the video pipeline for a [[Robot]].
*   `GET /drive-u/vin/{serialNumber}`: Fetches the VIN for a [[Robot]] by its serial number.
*   `GET /devices/state/latest`: Fetches the latest state for one or more devices.
*   `GET /devices/gps/batch`: Fetches the GPS location for a batch of devices.
*   `GET /devices`: Fetches a list of devices.
*   `GET /devices/{serial}`: Fetches details for a specific device.
