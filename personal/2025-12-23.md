Merchant issues meeting
* Create empty bot task whenever the bot gets loaded after the attempt is already done.
* Update the flow for pilots to check if there's a load whenever starting a trip
	* If there's a load for a return/pickup trip, Pilot flags that as needing a empty bot task
	* If there's no load for a delivery trip, Pilot flags that as missing cargo, then what